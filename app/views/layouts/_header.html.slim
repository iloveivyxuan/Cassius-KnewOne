header#header role="banner"
  nav.navbar.navbar-inverse.navbar-fixed-top role="navigation"
    .navbar-header
      = button_tag data: {toggle: "collapse", target: "#nav_main"}, class: "navbar-toggle" do
        span.sr-only "导航"
        span.fa.fa-bars
      = link_to root_url, class: "navbar-brand" do
        = logo
        span= brand

    #nav_main.collapse.navbar-collapse
      ul.nav.navbar-nav#nav_primary
        li.nav_group.dropdown
          = link_to "#", class: "dropdown-toggle", data: {toggle: "dropdown", url: categories_url} do
            i.fa.fa-magic.fa-fw
            | 产品
          = link_to "#", class: "dropdown-toggle", data: {toggle: "dropdown"} do
            b.caret
          ul.dropdown-menu role="menu"
            li = link_to_with_icon "分类", "fa fa-tags fa-fw", all_categories_url
            li = link_to_with_icon "最新", "fa fa-flash fa-fw", things_url
            li = link_to_with_icon "自营", "fa fa-shopping-cart fa-fw", things_url(self_run: true)
        li.visible-md.visible-lg
          - if user_signed_in?
            = link_to_with_icon "分享", "fa fa-plus fa-fw", new_thing_url
          - else
            = link_to_with_icon "分享", "fa fa-plus fa-fw", "#", data: {toggle: "modal", target: "#login-modal"}
        li
          = link_to_with_icon "小组", "fa fa-tint fa-fw", groups_url
        li
          = link_to_with_icon "客服", "fa fa-comment fa-fw", "#olark_chat", data: {hint: "fa-comment", url: asset_url('olark.js')}

      - if user_signed_in?
        .user_links.login.navbar-right
          #user.user_link.user data-name=current_user.name data-email=current_user.email data-id=current_user.id
            = link_to user_url(current_user), class: "avatar" do
              = image_tag current_user.avatar.url(:tiny)
            .more.dropdown
              = link_to "#", class: 'dropdown-toggle', data: {toggle: "dropdown"} do
                i.fa.fa-caret-down.fa_alone
              ul.dropdown-menu role="menu"
                li = link_to_with_icon '我的订单', 'fa fa-truck fa-fw', orders_url
                li = link_to_with_icon "控制面板", "fa fa-tachometer fa-fw", setting_root_url
                li = link_to_with_icon "安全登出", "fa fa-sign-out fa-fw", destroy_user_session_url, method: :delete

          = link_to cart_items_url, class: "user_link cart", title: "购物车" do
            i.fa.fa-shopping-cart.fa_alone
            span.badge.badge_success.cart_items_count= cart_items_count

          #notification_box.user_link.notification.dropdown
            = link_to notifications_url, id: 'notification_trigger', data: {url: notifications_url}, title: "#{unread_notifications_text}未读消息" do
              i.fa.fa-bell.fa_alone
              span#notification_count.badge.badge_primary= unread_notifications_count
            .dropdown_box
              header
                h5
                  i.fa.fa-bell
                  | 消息
              .dropdown_box_body
                ul.notifications
                .spinner
                  i.fa.fa-spinner.fa-spin.fa-2x
              footer
                = link_to_with_icon "设置", "fa fa-cog", edit_notification_settings_url, class: "pull-left"
                = link_to_with_icon "更多", "fa fa-ellipsis-h", notifications_url, class: "pull-right"

          - upmc = current_user.unread_private_messages_count
          = link_to dialogs_url, class: "user_link", title: "#{upmc}封未读私信" do
            i.fa.fa-envelope-o.fa_alone
            span.badge.badge_danger.private_messages_count= badge upmc
      - else
        .user_links.logout.navbar-right
          = link_to_with_icon "登录", "fa fa-sign-in", "#", class: "user_link", data: {toggle: "modal", target: "#login-modal"}

      = form_tag(search_url, method: "get", id: "navbar_search", class: "navbar-form navbar-right", role: "search") do
        .form-group.search
          input.form-control type="search" name="q" autocomplete="off" spellcheck="false"
          i.fa.fa-search
          i.fa.fa-times
  /-- Navbar --/

  .search_backdrop
  .search_candidate
    h5.search_title
      em.search_keyword
      | 的搜索结果：
    button.close aria-hidden="true" &times;

    .things.slideshow
      button.slideshow_control.left
        i.fa.fa-chevron-left
      button.slideshow_control.right
        i.fa.fa-chevron-right
      .slideshow_body

    .users.slideshow
      button.slideshow_control.left
        i.fa.fa-chevron-left
      button.slideshow_control.right
        i.fa.fa-chevron-right
      .slideshow_body
  /-- Search Candidate --/
