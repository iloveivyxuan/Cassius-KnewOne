$('#feeling_<%= @feeling.id %>')
.fadeOut('slow')
.remove();

var count = parseInt($('.feelings_count').text());
if (count > 1) {
  $('.feelings_count').text(count - 1);
} else {
  $('.feelings_count').text('');
}
