- provide :nav, :thing_feelings

= render 'form'

section.feelings
  = render 'sort', {thing: @thing}
  ul= render feelings
  = paginate feelings

- content_for :script do
  javascript:
    Making.InitFeelings();
