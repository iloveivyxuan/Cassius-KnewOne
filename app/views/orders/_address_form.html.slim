li.radio
  label
    = f.radio_button :address_id, :new, class: 'order_address_radio', checked: current_user.addresses.empty?
    | 使用新地址

  #new_address
    = f.fields_for :address do |address_form|
      .row
        .col-xs-12.col-md-3
          .form-group.select.address_province
            = address_form.select :province, options_for_select(Province.keys), {prompt: '请选择所在省/市'}, class: 'select form-control', required: true
        .col-xs-12.col-md-3
          = address_form.input :district, required: false, label: false, placeholder: '地区'
        .col-xs-12.col-md-5
          = address_form.input :street, required: false, label: false, placeholder: '街道地址'
      .row
        .col-xs-12.col-md-3
          = address_form.input :name, required: false, label: false, placeholder: '联系人'
        .col-xs-12.col-md-3
          = address_form.input :phone, required: false, label: false, placeholder: '电话'
        .col-xs-12.col-md-3
          = address_form.input :default, label: '设为首选', as: :boolean
