- provide :title, '购物车'
.carts_wrapper.widget
  header.page-header
    h2
      i.icon-shopping-cart
      | 购物车

  = form_tag new_order_path, method: :get
    fieldset
      ul.carts= render @cart_items
      p.total_price
        | 总计（不包含运费）
        strong
          i.icon-yen
          span= @total_price
      = button_tag type: "submit", class: "btn btn-success" do
        i.icon-credit-card
        | 结算

- content_for :script do
  javascript:
    $(function(){Making.CartCheck();});
