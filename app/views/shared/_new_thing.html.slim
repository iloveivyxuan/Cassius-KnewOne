#new-thing-modal.modal.fade
  .modal-dialog
    .modal-content
      .modal-header
        button.close type="button" data-dismiss="modal" aria-hidden="true" &times;
        h4.modal-title
          i.fa.fa-plus
          | 分享新产品

      .modal-body
        button.btn.btn-default [
          data-dismiss="modal"
          data-toggle="modal" data-target="#new-thing-from-url-modal"
        ]
          i.fa.fa-globe.fa-fw
          | 从网站收集

        = link_to_with_icon '本地上传',
          'fa fa-cloud-upload fa-fw', new_thing_path,
          class: 'btn btn-default'

#new-thing-from-url-modal.modal.fade
  = form_tag extract_url_things_path, method: :get, id: 'extract_url_form', remote: true do
    .modal-dialog
      .modal-content
        .modal-header
          .modal-title
            h4
              i.fa.fa-globe
              | 通过网页分享新产品

        .modal-body
          .form-group
            label for="new-thing-url" 产品地址
            input#new-thing-url.form-control required="required" type="url" placeholder="http://" name="url"

          .progress.progress-striped.active style="display: none"
            .progress-bar

          .alert.hidden
            | 很抱歉，这个地址我们不支持，您可以通过 #{link_to '本地上传', new_thing_path} 来分享此产品。

        .modal-footer
          button.btn.btn-default [
            type="button"
            data-dismiss="modal"
            data-toggle="modal" data-target="#new-thing-modal"
          ]
            i.fa.fa-chevron-left
            | 返回

          button.btn.btn-primary type="submit"
            i.fa.fa-crop
            | 抓取

#new-thing-similar-modal.modal.fade

#new-thing-edit-modal.modal.fade
