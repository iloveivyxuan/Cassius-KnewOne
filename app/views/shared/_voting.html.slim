- url = polymorphic_path([:vote, object.try(:thing) || object.try(:group), object])
= form_tag url, remote: true, class: 'voting' do
  = button_tag title: "赞", class: "btn btn-sm", disabled: !can?(:vote, object) do
    = present(object).lovers_count
